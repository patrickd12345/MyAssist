node_modules

# Cursor / local debug artifacts (may contain OAuth query strings)
.cursor/debug*.log

# Next.js build output (delete and rebuild if webpack chunk errors e.g. Cannot find module './432.js')
apps/web/.next
apps/web/tests/e2e/.playwright-users.json
apps/web/.infisical.json
